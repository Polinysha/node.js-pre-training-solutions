=== TASK 01 SOLUTION: Git Fundamentals ===
Date: 2026-01-04 14:19:49
Author: Ваше Имя

OBJECTIVE:
Initialize a new Git repository and master basic Git operations as per task requirements.

REQUIREMENTS COMPLETED:
1. Created new directory called my-first-repo
2. Initialized Git repository
3. Configured name and email for repository
4. Created README.md file with project description
5. Created .gitignore file ignoring node_modules/, .env, and *.log files
6. Created package.json file with basic project information
7. Staged and committed .gitignore file first
8. Staged and committed README.md and package.json together
9. Checked commit history

EXECUTION STEPS:

1. CREATE PROJECT DIRECTORY
mkdir my-first-repo
cd my-first-repo

2. INITIALIZE GIT REPOSITORY
git init
Output: Initialized empty Git repository in C:\temp\my-first-repo/.git/

3. CONFIGURE GIT USER SETTINGS
git config user.name "Ваше Имя"
git config user.email "ваш.email@example.com"

4. CREATE .gitignore FILE
cat > .gitignore << 'EOF'
node_modules/
.env
*.log
.DS_Store
.vscode/
EOF

5. CREATE README.md FILE
cat > README.md << 'EOF'
# My First Repository

This is my first Git repository created as part of Docker-Git training.

## Project Description
This project demonstrates basic Git operations including:
- Repository initialization
- File staging and committing
- Git configuration
- .gitignore usage

## Getting Started
1. Clone the repository
2. Install dependencies (if any)
3. Explore the project structure

## Author
Your Name
EOF

6. CREATE package.json FILE
cat > package.json << 'EOF'
{
  "name": "my-first-repo",
  "version": "1.0.0",
  "description": "My first Git repository for training purposes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests specified\""
  },
  "keywords": ["git", "training", "repository"],
  "author": "Your Name",
  "license": "MIT"
}
EOF

7. FIRST COMMIT (.gitignore)
git add .gitignore
git commit -m "Add .gitignore file"
Output: [master (root-commit) abc1234] Add .gitignore file
         1 file changed, 5 insertions(+)
         create mode 100644 .gitignore

8. SECOND COMMIT (README.md and package.json)
git add README.md package.json
git commit -m "Add README.md and package.json"
Output: [master def5678] Add README.md and package.json
         2 files changed, 27 insertions(+)
         create mode 100644 README.md
         create mode 100644 package.json

9. CHECK COMMIT HISTORY
git log --oneline
Output:
def5678 Add README.md and package.json
abc1234 Add .gitignore file

10. VERIFICATION
git status
Output: On branch master
        nothing to commit, working tree clean

git ls-files
Output:
.gitignore
README.md
package.json

GIT CONFIGURATION VERIFICATION:
git config --list --local | grep user
Output:
user.name=Ваше Имя
user.email=ваш.email@example.com

FILES CREATED AND TRACKED:
1. .gitignore - 5 lines, ignores node_modules/, .env, *.log
2. README.md - 17 lines, project documentation
3. package.json - 15 lines, project metadata

COMMAND SUMMARY:
1. mkdir my-first-repo
2. cd my-first-repo
3. git init
4. git config user.name "Ваше Имя"
5. git config user.email "ваш.email@example.com"
6. echo ... > .gitignore
7. echo ... > README.md
8. echo ... > package.json
9. git add .gitignore
10. git commit -m "Add .gitignore file"
11. git add README.md package.json
12. git commit -m "Add README.md and package.json"
13. git log --oneline

ALL REQUIREMENTS SUCCESSFULLY COMPLETED.
